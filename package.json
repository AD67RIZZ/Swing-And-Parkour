{
  "name": "neon-grapple-rush",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Original 3D rooftop grappling race with Cloudflare Durable Object multiplayer.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:server": "wrangler dev",
    "build": "tsc --noEmit && vite build",
    "check": "tsc --noEmit && tsc --noEmit -p worker/tsconfig.json",
    "test:smoke": "esbuild work/input-smoke.ts --bundle --platform=node --format=cjs --outfile=work/input-smoke.cjs --log-level=warning && node work/input-smoke.cjs && esbuild work/collision-smoke.ts --bundle --platform=node --format=cjs --outfile=work/collision-smoke.cjs --log-level=warning && node work/collision-smoke.cjs",
    "preview": "vite preview --host 0.0.0.0",
    "deploy:server": "wrangler deploy"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260728.1",
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "wrangler": "^4.27.0"
  },
  "engines": {
    "node": ">=20"
  },
  "allowScripts": {
    "esbuild@0.28.1": true,
    "workerd@1.20260722.1": true
  }
}
